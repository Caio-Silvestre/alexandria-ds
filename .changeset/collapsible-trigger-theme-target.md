---
'@pharos-ds/core': patch
---

[feat] Collapsible: expose the trigger button as a distinct theming target (`pharos-collapsible-trigger`) so themes can style the trigger independently from the content — e.g. a heading font on the trigger while the content keeps the body font.
@freddymeta
